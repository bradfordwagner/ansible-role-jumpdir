# bradfordwagner.ansible-role-jumpdir

## Updating the version

Version is controlled by `jd_ver` in `defaults/main.yml`.

Check for the latest release at: https://github.com/bradfordwagner/go-cli-jumpdir/releases

```yaml
jd_ver: vX.Y.Z
```
